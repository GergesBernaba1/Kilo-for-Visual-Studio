<UserControl x:Class="Kilo.VisualStudio.Extension.UI.KiloAgentManagerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#1E1E1E">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="12,12,12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Agent Manager" FontSize="16" FontWeight="SemiBold" Foreground="#EEEEEE" VerticalAlignment="Center"/>
            <Button x:Name="RefreshButton" Grid.Column="1" Content="↻" Width="28" Height="28"
                    Background="#2D2D2D" Foreground="#CCCCCC" BorderThickness="0" Cursor="Hand"
                    Click="RefreshButton_Click" ToolTip="Refresh agents"/>
        </Grid>

        <!-- Agent List -->
        <ListBox x:Name="AgentListBox" Grid.Row="1" Margin="12,0,12,12"
                 Background="#1E1E1E" Foreground="#CCCCCC" BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Border Width="32" Height="32" CornerRadius="16" Background="{Binding Color}" Margin="0,0,12,0">
                            <TextBlock Text="{Binding Icon}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="#EEEEEE"/>
                            <TextBlock Text="{Binding Description}" FontSize="11" Foreground="#9CA3AF" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <TextBlock Grid.Column="2" Text="{Binding Status}" FontSize="11" Foreground="#17A2B8" VerticalAlignment="Center"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="12,0,12,12" HorizontalAlignment="Right">
            <Button x:Name="NewAgentButton" Content="New Agent" Width="90" Height="28" Margin="0,0,8,0"
                    Background="#007ACC" Foreground="White" BorderThickness="0" Cursor="Hand"
                    Click="NewAgentButton_Click"/>
            <Button x:Name="CloseButton" Content="Close" Width="70" Height="28"
                    Background="#2D2D2D" Foreground="#CCCCCC" BorderThickness="0" Cursor="Hand"
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</UserControl>
