<UserControl x:Class="Kilo.VisualStudio.Extension.UI.KiloDiffViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#252526" Padding="12,8" BorderThickness="0,0,0,1" BorderBrush="#3C3C3C">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="Kilo Diff Viewer" FontSize="14" FontWeight="SemiBold" Foreground="#EEEEEE" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="PreviousButton" Content="◀ Prev" Width="70" Height="24" Margin="0,0,4,0"
                            Background="#3C3C3C" Foreground="#CCCCCC" BorderThickness="0" FontSize="11" Click="PreviousButton_Click"/>
                    <Button x:Name="NextButton" Content="Next ▶" Width="70" Height="24" Margin="0,0,4,0"
                            Background="#3C3C3C" Foreground="#CCCCCC" BorderThickness="0" FontSize="11" Click="NextButton_Click"/>
                    <TextBlock x:Name="FileCountLabel" Text="0 of 0" Foreground="#9CA3AF" FontSize="11" VerticalAlignment="Center" Margin="8,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Diff content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left (Original) -->
            <Border Grid.Column="0" Background="#1E1E1E" BorderBrush="#3C3C3C" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#2D2D2D" Padding="8,4">
                        <TextBlock x:Name="LeftFileLabel" Text="Original" Foreground="#9CA3AF" FontSize="11" FontWeight="SemiBold"/>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <TextBox x:Name="LeftContentTextBox"
                                 IsReadOnly="True"
                                 FontFamily="Consolas" FontSize="11"
                                 Background="#1E1E1E" Foreground="#D4D4D4"
                                 BorderThickness="0"
                                 AcceptsReturn="True"
                                 TextWrapping="NoWrap"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" Width="4" Background="#3C3C3C" HorizontalAlignment="Center"/>

            <!-- Right (Modified) -->
            <Border Grid.Column="2" Background="#1E1E1E">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border Grid.Row="0" Background="#2D2D2D" Padding="8,4">
                        <TextBlock x:Name="RightFileLabel" Text="Modified" Foreground="#9CA3AF" FontSize="11" FontWeight="SemiBold"/>
                    </Border>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <TextBox x:Name="RightContentTextBox"
                                 IsReadOnly="True"
                                 FontFamily="Consolas" FontSize="11"
                                 Background="#1E1E1E" Foreground="#D4D4D4"
                                 BorderThickness="0"
                                 AcceptsReturn="True"
                                 TextWrapping="NoWrap"/>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Footer actions -->
        <Border Grid.Row="2" Background="#252526" Padding="12,8" BorderThickness="0,1,0,0" BorderBrush="#3C3C3C">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusLabel" Grid.Column="0" Text="No changes" Foreground="#9CA3AF" FontSize="11" VerticalAlignment="Center"/>
                <Button x:Name="RevertButton" Grid.Column="1" Content="Revert" Width="80" Height="26" Margin="0,0,6,0"
                        Background="#7F1D1D" Foreground="#FCA5A5" BorderThickness="0" FontSize="11" Click="RevertButton_Click"/>
                <Button x:Name="ApplyButton" Grid.Column="2" Content="Apply Changes" Width="110" Height="26"
                        Background="#0E639C" Foreground="White" BorderThickness="0" FontSize="11" Click="ApplyButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>